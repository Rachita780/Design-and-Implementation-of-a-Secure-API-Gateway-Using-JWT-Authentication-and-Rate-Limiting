# Secure-API-Gateway-with-Rate-Limiting-JWT-Auth
A Flask-based secure API gateway that protects REST APIs using JWT authentication and rate limiting. The system issues tokens after login, validates requests using JWT, and restricts excessive API calls to prevent unauthorized access and abuse, demonstrating core API security best practices.
